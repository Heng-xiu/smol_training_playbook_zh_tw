# 🚀 Docsify + GitHub Pages 部署指南

## ✅ 已完成的準備工作

- [x] 創建 `index.html` - Docsify 核心配置文件
- [x] 創建 `_sidebar.md` - 側邊欄導航（41個章節）
- [x] 創建 `_coverpage.md` - 美觀的封面頁
- [x] 創建 `.nojekyll` - GitHub Pages 配置文件
- [x] GitHub 倉庫已創建並上傳基本文件

---

## 📋 立即部署（3個步驟）

### 步驟 1: 提交新文件到 Git

在終端機中執行（複製貼上即可）：

```bash
cd /Users/xiuxiu/Downloads/translation_smol_training_playbook/smol_training_playbook_zh_tw

# 添加所有新文件
git add index.html _sidebar.md _coverpage.md .nojekyll

# 查看即將提交的文件
git status

# 創建提交
git commit -m "feat: 添加 Docsify 配置文件

- 添加 index.html（Docsify 核心配置）
- 添加 _sidebar.md（側邊欄導航）
- 添加 _coverpage.md（封面頁）
- 添加 .nojekyll（GitHub Pages 配置）

功能特性：
✨ 全文搜尋
✨ 程式碼高亮
✨ 複製程式碼按鈕
✨ 分頁導航
✨ 圖片縮放
✨ 回到頂部
✨ 響應式設計"

# 推送到 GitHub
git push origin main
```

### 步驟 2: 啟用 GitHub Pages

1. 前往您的 GitHub 倉庫: https://github.com/Heng-xiu/smol_training_playbook_zh_tw

2. 點擊倉庫頂部的 **⚙️ Settings**（設定）

3. 在左側選單中找到 **📄 Pages**（在 "Code and automation" 區塊下）

4. 在 **Source** 區域：
   - Branch: 選擇 `main`
   - Folder: 選擇 `/ (root)`
   - 點擊 **Save**

5. 等待 1-2 分鐘，GitHub 會顯示您的網站 URL:
   ```
   https://heng-xiu.github.io/smol_training_playbook_zh_tw/
   ```

### 步驟 3: 驗證部署

1. 等待部署完成（通常 1-2 分鐘）

2. 打開瀏覽器訪問：
   ```
   https://heng-xiu.github.io/smol_training_playbook_zh_tw/
   ```

3. 您應該會看到：
   - ✅ 美觀的封面頁
   - ✅ 可點擊的「開始閱讀」按鈕
   - ✅ 完整的側邊欄導航
   - ✅ 搜尋功能

---

## 🎨 功能介紹

您的文檔網站現在擁有以下功能：

### 📚 核心功能
- ✅ **封面頁**: 精美的首頁介紹
- ✅ **側邊欄導航**: 41個章節，可摺疊展開
- ✅ **全文搜尋**: 即時搜尋所有內容
- ✅ **響應式設計**: 手機、平板、電腦完美適配

### 💻 程式碼功能
- ✅ **語法高亮**: 支援 Python, Bash, YAML, JSON, Markdown
- ✅ **複製按鈕**: 一鍵複製程式碼
- ✅ **行號顯示**: 程式碼區塊顯示行號

### 🎯 閱讀體驗
- ✅ **分頁導航**: 上一頁/下一頁按鈕
- ✅ **回到頂部**: 自動顯示返回頂部按鈕
- ✅ **圖片縮放**: 點擊圖片放大查看
- ✅ **字數統計**: 顯示閱讀時間

### 🎨 視覺效果
- ✅ **Vue 主題**: 清爽美觀的界面
- ✅ **中文優化**: 適合繁體中文閱讀的字體和排版
- ✅ **表格美化**: 清晰易讀的表格樣式

---

## 🔧 未來更新流程

當您需要更新文檔內容時：

```bash
cd /Users/xiuxiu/Downloads/translation_smol_training_playbook/smol_training_playbook_zh_tw

# 修改 Markdown 文件後...

git add .
git commit -m "docs: 更新內容描述"
git push

# GitHub Pages 會自動更新！
```

---

## 📝 自訂配置（可選）

### 更改主題顏色

編輯 `index.html`，找到：
```css
--theme-color: #FF6B6B;
```
修改為您喜歡的顏色（例如：`#42b983` 綠色）

### 添加 Google Analytics

編輯 `index.html`，取消註釋並替換 GA ID：
```html
<script>
  window.ga=window.ga||function(){(ga.q=ga.q||[]).push(arguments)};ga.l=+new Date;
  ga('create', 'UA-XXXXX-Y', 'auto');  // 替換成您的 GA ID
  ga('send', 'pageview');
</script>
```

### 修改封面頁

編輯 `_coverpage.md` 文件，自訂：
- Logo 圖片
- 標題和副標題
- 按鈕連結
- 背景顏色

---

## 🐛 常見問題

### Q: 推送後網站沒有更新？

A:
1. 檢查 GitHub Actions 狀態（倉庫頁面的 Actions 標籤）
2. 清除瀏覽器快取（Ctrl+F5 或 Cmd+Shift+R）
3. 等待 2-3 分鐘再重新整理

### Q: 側邊欄沒有顯示？

A: 檢查：
1. `_sidebar.md` 文件是否存在
2. `index.html` 中 `loadSidebar: true` 是否設定
3. 文件路徑是否正確

### Q: 搜尋功能不工作？

A:
1. 確認 `index.html` 中引入了 search 插件
2. 清除瀏覽器快取重試
3. 檢查瀏覽器控制台是否有錯誤

### Q: 圖片無法顯示？

A:
1. 確認圖片檔案已推送到 GitHub
2. 使用相對路徑（不要用絕對路徑）
3. 檢查文件名大小寫是否正確

### Q: 想要添加自訂域名？

A:
1. 在倉庫根目錄創建 `CNAME` 文件
2. 內容為您的域名（例如：`docs.yourdomain.com`）
3. 在域名 DNS 設定中添加 CNAME 記錄指向 `heng-xiu.github.io`

---

## 📚 進階功能

### 添加頂部導航欄

創建 `_navbar.md` 文件：
```markdown
* [首頁](/)
* [GitHub](https://github.com/Heng-xiu/smol_training_playbook_zh_tw)
* [原文](https://huggingfacetb-smol-training-playbook.hf.space/)
```

然後在 `index.html` 中設定：
```javascript
loadNavbar: true,
```

### 啟用離線模式

在 `index.html` 中添加：
```javascript
$docsify.plugins = [
  function(hook, vm) {
    hook.beforeEach(function(html) {
      // 離線支援邏輯
    });
  }
];
```

---

## 🎉 完成！

您的文檔網站已經成功部署到：

```
https://heng-xiu.github.io/smol_training_playbook_zh_tw/
```

現在您可以：
- ✅ 分享連結給朋友和社群
- ✅ 在社交媒體上發布
- ✅ 提交到相關技術社群
- ✅ 添加到您的個人網站

---

**祝您分享順利！** 🚀

如有任何問題，請參考：
- Docsify 官方文檔: https://docsify.js.org/
- GitHub Pages 文檔: https://docs.github.com/pages
